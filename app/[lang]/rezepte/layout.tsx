import { ReactNode } from "react"

const RecipeLayout = async ({ children }: { children: ReactNode }) => {

    return (
        <>
            {children}
        </>
    )
}

export default RecipeLayout